{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start DNSSEC validator",
            "program": "${workspaceFolder}/dnssec-validator.js",
            "console": "integratedTerminal"
        }
    ]
}